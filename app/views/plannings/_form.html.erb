<%= form_with(model: planning, local: true) do |form| %>
  <% if planning.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(planning.errors.count, "error") %> prohibited this planning from being saved:</h2>

      <ul>
      <% planning.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :open_hour %>
    <%= form.datetime_select :open_hour %>
  </div>

  <div class="field">
    <%= form.label :close_hour %>
    <%= form.datetime_select :close_hour %>
  </div>

  <div class="field">
    <%= form.label :bakery_id %>
    <%= form.text_field :bakery_id %>
  </div>

  <div class="field">
    <%= form.label :week_id %>
    <%= form.text_field :week_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
